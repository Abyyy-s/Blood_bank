const API_URL='';let allRequests=[];let currentUser=null;
async function getCurrentUser(){try{const resp=await fetch(`${API_URL}/api/me`);if(!resp.ok)throw new Error('Not authenticated');currentUser=await resp.json();window.lifeLinkUser=currentUser;return currentUser;}catch(e){console.error(e);return null;}}
async function loadHospitals(){try{const response=await fetch(`${API_URL}/hospitals`);if(!response.ok)throw new Error('Failed');const hospitals=await response.json();const select=document.querySelector('select[name="hospital_id"]');if(!select)return;select.querySelectorAll('option:not(:first-child)').forEach(o=>o.remove());hospitals.forEach(h=>{const option=document.createElement('option');option.value=h.hospital_id;option.textContent=`${h.hospital_name} — ${h.location||'Location unavailable'}`;select.appendChild(option);});}catch(error){console.error(error);}}
async function loadRequests(){try{const status=document.getElementById('filterStatus')?.value||'';const bloodGroup=document.getElementById('filterBloodGroup')?.value||'';const params=new URLSearchParams();if(status)params.append('status',status);if(bloodGroup)params.append('blood_group',bloodGroup);const response=await fetch(params.toString()?`${API_URL}/requests?${params}`:`${API_URL}/requests`);if(!response.ok)throw new Error('Failed to load requests');allRequests=await response.json();renderRequestSummary(allRequests);displayRequests(allRequests);}catch(error){console.error(error);showAlert('Requests could not be refreshed.','error');}}
function renderRequestSummary(requests){const open=requests.filter(r=>['Pending','Approved'].includes(r.status)).length;const critical=requests.filter(r=>r.urgency_level==='Critical'&&['Pending','Approved'].includes(r.status)).length;const fulfilled=requests.filter(r=>r.status==='Fulfilled').length;const units=requests.reduce((s,r)=>s+(Number(r.quantity_units)||0),0);document.querySelectorAll('#requestSummary .op-metric-value').forEach((el,i)=>el.textContent=[open,critical,fulfilled,units][i].toLocaleString('en-IN'));}
function displayRequests(requests){const tableBody=document.getElementById('requestsTableBody');if(!tableBody)return;tableBody.innerHTML='';if(!requests?.length){const message=currentUser?.role==='Hospital'?'You have not made any requests yet.':'No requests match the current filters.';tableBody.innerHTML=`<tr><td colspan="8"><div class="op-empty"><i class="fas fa-clipboard-check" aria-hidden="true"></i><strong>${message}</strong><br><span>${currentUser?.role==='Hospital'?'Create a request when your hospital needs blood.':'Try another filter or create a new request.'}</span></div></td></tr>`;return;}const urgencyRank={Critical:0,High:1,Medium:2,Low:3};[...requests].sort((a,b)=>(urgencyRank[a.urgency_level]??9)-(urgencyRank[b.urgency_level]??9)).forEach(req=>{const row=document.createElement('tr');if(req.urgency_level==='Critical'&&['Pending','Approved'].includes(req.status))row.classList.add('ll-row-focus');let actionBtn='';if(currentUser?.role==='Hospital'){actionBtn=req.status==='Pending'?`<div class="op-action-group"><button class="op-action op-action-danger" onclick="cancelRequest(${req.request_id})">Cancel</button></div>`:'<span style="color:var(--op-muted)">—</span>';}else if(req.status==='Pending'){actionBtn=`<div class="op-action-group"><button class="op-action op-action-primary" onclick="openUpdateModal(${req.request_id}, '${req.status}')">Fulfill</button><button class="op-action op-action-danger" onclick="rejectRequest(${req.request_id})">Reject</button></div>`;}else if(req.status==='Approved'){actionBtn=`<div class="op-action-group"><button class="op-action op-action-primary" onclick="openUpdateModal(${req.request_id}, '${req.status}')">Fulfill</button><button class="op-action" onclick="openUpdateModal(${req.request_id}, '${req.status}')">Update</button></div>`;}else{actionBtn=`<div class="op-action-group"><button class="op-action" onclick="openUpdateModal(${req.request_id}, '${req.status}')">Update</button></div>`;}row.innerHTML=`<td><span class="op-id">#${req.request_id}</span></td><td><span class="op-name">${req.hospital_name}</span><br><span style="color:var(--op-muted);font-size:.68rem">${req.location||'N/A'}</span></td><td><span class="op-blood">${req.blood_group}</span><br><span class="op-badge op-badge-blue" style="margin-top:5px">${req.component_type||'Whole Blood'}</span></td><td><span class="op-qty">${Number(req.quantity_units).toLocaleString('en-IN')}</span></td><td>${getUrgencyBadge(req.urgency_level)}</td><td>${getStatusBadge(req.status)}</td><td>${formatDate(req.request_date)}</td><td>${actionBtn}</td>`;tableBody.appendChild(row);});}
function openCreateModal(){const modal=document.getElementById('createModal');if(!modal)return;modal.classList.add('active');document.getElementById('createForm')?.reset();const group=document.querySelector('select[name="hospital_id"]')?.closest('.form-group');if(group)group.style.display=currentUser?.role==='Hospital'?'none':'';}
function closeCreateModal(){document.getElementById('createModal')?.classList.remove('active');}
async function createRequest(event){event.preventDefault();const form=document.getElementById('createForm');const fd=new FormData(form);const data={blood_group:fd.get('blood_group'),component_type:fd.get('component_type'),quantity_units:parseInt(fd.get('quantity_units'),10),urgency_level:fd.get('urgency_level')};if(currentUser?.role!=='Hospital'){const id=fd.get('hospital_id');if(!id){showAlert('Select a hospital before submitting.','error');return;}data.hospital_id=parseInt(id,10);}if(!data.blood_group||!data.component_type||!data.quantity_units||!data.urgency_level){showAlert('Complete all required request fields.','error');return;}const submitBtn=form.querySelector('button[type="submit"]');try{submitBtn.disabled=true;submitBtn.setAttribute('aria-busy','true');submitBtn.textContent='Submitting…';const response=await fetch(`${API_URL}/requests`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)});const result=await response.json();if(response.ok){closeCreateModal();await loadRequests();showAlert(result.warning||'Blood request submitted successfully.',result.warning?'warning':'success');}else showAlert(result.error||'Error creating request','error');}catch(error){console.error(error);showAlert('Network error. Please try again.','error');}finally{submitBtn.disabled=false;submitBtn.removeAttribute('aria-busy');submitBtn.textContent='Create Request';}}
async function rejectRequest(id){if(!window.confirm('Reject this request?'))return;await updateStatus(id,'Rejected');}
async function cancelRequest(id){if(!window.confirm('Cancel this request?'))return;try{const response=await fetch(`${API_URL}/requests/${id}`,{method:'DELETE'});const result=await response.json();if(response.ok){showAlert('Request cancelled.','success');await loadRequests();}else showAlert(result.error||'Could not cancel request','error');}catch(e){showAlert('Network error. Please try again.','error');}}
function openUpdateModal(id,status){const modal=document.getElementById('updateModal');if(!modal)return;document.getElementById('updateRequestId').value=id;const select=document.querySelector('#updateForm select[name="status"]');if(select)select.value=status;modal.classList.add('active');}
function closeUpdateModal(){document.getElementById('updateModal')?.classList.remove('active');}
async function updateRequestStatus(event){event.preventDefault();const form=document.getElementById('updateForm');const data=new FormData(form);await updateStatus(data.get('request_id'),data.get('status'));closeUpdateModal();}
async function updateStatus(id,newStatus){try{const response=await fetch(`${API_URL}/requests/${id}`,{method:'PUT',headers:{'Content-Type':'application/json'},body:JSON.stringify({status:newStatus})});const result=await response.json();if(response.ok){await loadRequests();showAlert(result.warning||result.message||'Request status updated.',result.warning?'warning':'success');}else showAlert(result.error||'Error updating request','error');}catch(error){console.error(error);showAlert('Network error. Please try again.','error');}}
function getUrgencyBadge(u){const map={Critical:'<span class="op-badge op-badge-danger"><i class="fas fa-bolt"></i> Critical</span>',High:'<span class="op-badge op-badge-warn"><i class="fas fa-arrow-up"></i> High</span>',Medium:'<span class="op-badge op-badge-blue"><i class="fas fa-minus"></i> Medium</span>',Low:'<span class="op-badge op-badge-good"><i class="fas fa-arrow-down"></i> Low</span>'};return map[u]||`<span class="op-badge">${u||'—'}</span>`;}
function getStatusBadge(s){const map={Pending:'<span class="op-badge op-badge-warn"><i class="fas fa-hourglass-half"></i> Pending</span>',Approved:'<span class="op-badge op-badge-blue"><i class="fas fa-check"></i> Approved</span>',Fulfilled:'<span class="op-badge op-badge-good"><i class="fas fa-check-double"></i> Fulfilled</span>',Rejected:'<span class="op-badge op-badge-danger"><i class="fas fa-xmark"></i> Rejected</span>',Cancelled:'<span class="op-badge op-badge-danger"><i class="fas fa-xmark"></i> Cancelled</span>'};return map[s]||`<span class="op-badge">${s||'—'}</span>`;}
function formatDate(d){return d?new Date(d).toLocaleDateString('en-IN',{day:'numeric',month:'short',year:'numeric'}):'N/A';}
function showAlert(message,type='info'){if(typeof window.lifeLinkToast==='function')return window.lifeLinkToast(message,type);alert(message);}
async function init(){document.getElementById('filterStatus')?.addEventListener('change',loadRequests);document.getElementById('filterBloodGroup')?.addEventListener('change',loadRequests);await getCurrentUser();if(!currentUser){showAlert('Session expired. Please log in again.','error');return;}if(currentUser.role!=='Hospital')await loadHospitals();await loadRequests();setInterval(loadRequests,30000);}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init,{once:true});else init();
window.addEventListener('click',event=>{const create=document.getElementById('createModal'),update=document.getElementById('updateModal');if(create&&event.target===create)closeCreateModal();if(update&&event.target===update)closeUpdateModal();});
